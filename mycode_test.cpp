#include "mycode.h"
#include <cassert>

// This program should test the code contained in MyClass.
// It should return 0 if the test succeeds, 1 if the test fails. 
int main(){

  MyClass::isInE("");
  
  assert(false);

  return 0;
}
